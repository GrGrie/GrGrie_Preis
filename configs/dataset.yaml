path: ..  # Resolve back to repo root so YOLO finds the generated lists

# Each entry below points to a text file with absolute image paths that live
# under data/originals. They are regenerated by make_pathlists() in model.py.
train: configs/lists/train.txt
val:   configs/lists/val.txt
test:  configs/lists/test.txt

names:
  0: Product
